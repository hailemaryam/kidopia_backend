name: Deploy Game Portal on gamestation

on:
  push:
    branches: ["main"]
    paths:
      - "**"

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Git Pull and Migrate
        uses: appleboy/ssh-action@v0.1.4
        with:
          host: ${{ secrets.VPS_HOST2 }}
          username: ${{ secrets.VPS_USER2 }}
          key: ${{ secrets.SSH_PRIVATE_KEY2 }}
          script: |
            cd /home/frappe/frappe-bench/apps/game_portal/
            git pull
            bench --site kidopia.memby.online migrate
            bench --site gamestation.memby.online migrate

      - name: Build and Restart
        uses: appleboy/ssh-action@v0.1.4
        with:
          host: ${{ secrets.VPS_HOST2 }}
          username: ${{ secrets.VPS_USER2 }}
          key: ${{ secrets.SSH_PRIVATE_KEY2 }}
          script: |
            cd /home/frappe/frappe-bench/
            bench build
            bench restart

